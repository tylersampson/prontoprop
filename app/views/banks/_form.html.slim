= bootstrap_form_for @bank do |f|
  = f.text_field :name
  = f.submit I18n.t('save'), class: 'btn btn-primary'
  - if @bank.new_record?
    = f.submit I18n.t('save_and_new'), name: 'save_and_new', class: 'btn btn-info'
